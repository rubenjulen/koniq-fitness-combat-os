# Productie: echte Postgres/Supabase. Leeg laten = ingebedde PGlite (lokaal/demo).
DATABASE_URL=

# PGlite datadir (buiten OneDrive houden i.v.m. sync-locks). Default = OS tmpdir.
PGLITE_DIR=

# Zet op "false" om auto-seed in productie uit te schakelen.
SEED_ON_BOOT=true
